---
title: "2장 실습환경구축"
date: 2026-08-02
tags: ["메가존클라우드 MSP 솔루션 아키텍트", "쿠버네티스"]
is_public: true
draft: false
---

# 2장. 실습환경구축

# 전체 구성 요약

```
[ k8s-cp ]        [ k8s-w1 ]        [ k8s-w2 ]
 Controller        Worker            Worker
   kube-apiserver
   etcd
   scheduler
   controller-manager

 ───────────── Pod Network (Flannel : 10.244.0.0/16) ─────────────
```

* OS: **Ubuntu Server 22.04**

* Kubernetes: **v1.34.x (stable)**

* CRI: **containerd**

* CNI: **Flannel**

* 설치 방식: **kubeadm**

---

# 1️⃣ 사전 준비 (모든 노드 공통)

## 1-1. 호스트명 설정 (권장)

```
# Controller
sudo hostnamectl set-hostname k8s-cp

# Worker1
sudo hostnamectl set-hostname k8s-w1

# Worker2
sudo hostnamectl set-hostname k8s-w2
```

확인:

```
hostname
```

---

## 1-2. Swap 비활성화 (★ 매우 중요)

```
sudo swapoff -a
sudo sed -i '/swap/ s/^/#/' /etc/fstab
```

> 📌 kubelet은 메모리 관리 안정성을 위해 swap이 켜져 있으면 실행을 거부함

---

# 2️⃣ 공통 설치 스크립트 (모든 노드)

아래 스크립트는 **Controller / Worker 모두 동일하게 실행**

## 2-1. 공통 스크립트 생성

```
cat <<'BASH' | sudo tee /root/00-common.sh
#!/usr/bin/env bash
set -e

K8S_VERSION="1.34.3-1.1"

echo "[1] 커널 모듈 로드"
cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF
sudo modprobe overlay
sudo modprobe br_netfilter

echo "[2] sysctl 설정"
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF
sudo sysctl --system

echo "[3] 필수 패키지"
sudo apt update -y
sudo apt install -y ca-certificates curl gnupg apt-transport-https

echo "[4] containerd 설치"
sudo apt install -y containerd

echo "[5] containerd cgroup 설정"
sudo mkdir -p /etc/containerd
containerd config default | sudo tee /etc/containerd/config.toml
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml
sudo systemctl restart containerd
sudo systemctl enable containerd

echo "[6] Kubernetes v1.34 repo 추가"
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.34/deb/Release.key \
 | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg

echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] \
https://pkgs.k8s.io/core:/stable:/v1.34/deb/ /" \
 | sudo tee /etc/apt/sources.list.d/kubernetes.list

sudo apt update -y

echo "[7] kubeadm / kubelet / kubectl 설치"
sudo apt install -y kubelet=${K8S_VERSION} kubeadm=${K8S_VERSION} kubectl=${K8S_VERSION}
sudo apt-mark hold kubelet kubeadm kubectl

echo "[8] kubelet 활성화"
sudo systemctl enable kubelet

echo "공통 설정 완료"
BASH
```

실행:

```
sudo chmod +x /root/00-common.sh
sudo /root/00-common.sh
```

---

# 3️⃣ Controller 초기화 (Controller 노드만)

## 3-1. kubeadm init

```
sudo kubeadm init \
  --apiserver-advertise-address=$(hostname -I | awk '{print $1}') \
  --pod-network-cidr=10.244.0.0/16
```

> 📌 강의 설명
>
> * `pod-network-cidr`는 **CNI와 반드시 일치**
>
> * Flannel 기본값 = `10.244.0.0/16`

---

## 3-2. kubectl 설정

```
mkdir -p $HOME/.kube
sudo cp /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
```

확인:

```
kubectl get nodes
```

---

## 3-3. CNI(Flannel) 설치

```
kubectl apply -f https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml
```

확인:

```
kubectl get pods -n kube-system
```

---

## 3-4. Worker Join 명령 생성

```
kubeadm token create --print-join-command
```

출력 예:

```
kubeadm join 192.168.10.10:6443 --token xxxx \
 --discovery-token-ca-cert-hash sha256:yyyy
```

---

# 4️⃣ Worker 노드 Join (Worker 2대)

각 Worker에서 **위 join 명령 그대로 실행**

```
sudo kubeadm join <controller-ip>:6443 --token ... --discovery-token-ca-cert-hash ...
```

---

# 5️⃣ 최종 확인 (Controller)

```
kubectl get nodes -o wide
```

정상 상태:

```
NAME      STATUS   ROLES           VERSION
k8s-cp    Ready    control-plane   v1.34.3
k8s-w1    Ready    <none>          v1.34.3
k8s-w2    Ready    <none>          v1.34.3
```

---

### ❌ 실습 중 가장 많은 오류

| 원인 | 증상 |
| --- | --- |
| swap 미비활성 | kubeadm init 실패 |
| cgroup 불일치 | kubelet CrashLoop |
| CIDR 불일치 | 노드 NotReady |
| IP 잘못 지정 | Worker join 실패 |

---

[Windows를 Kubernetes 클라이언트로 설정](2%EC%9E%A5%20%EC%8B%A4%EC%8A%B5%ED%99%98%EA%B2%BD%EA%B5%AC%EC%B6%95/Windows%EB%A5%BC%20Kubernetes%20%ED%81%B4%EB%9D%BC%EC%9D%B4%EC%96%B8%ED%8A%B8%EB%A1%9C%20%EC%84%A4%EC%A0%95)[kubens 설치](2%EC%9E%A5%20%EC%8B%A4%EC%8A%B5%ED%99%98%EA%B2%BD%EA%B5%AC%EC%B6%95/kubens%20%EC%84%A4%EC%B9%98)[kube-ops-view 설치](2%EC%9E%A5%20%EC%8B%A4%EC%8A%B5%ED%99%98%EA%B2%BD%EA%B5%AC%EC%B6%95/kube-ops-view%20%EC%84%A4%EC%B9%98)