---
title: "실습 2 지식 기반 + 운영 상태 조회 기반 챗봇"
date: 2026-08-02
tags: ["메가존클라우드 MSP 솔루션 아키텍트", "클라우드 자동화를 위한 파이썬 프로그래밍", "9장 Amazon Bedrock과 RAG를 이용한 운영 시스템 챗봇 만들기"]
is_public: true
draft: false
---

# 실습 2. 지식 기반 + 운영 상태 조회 기반 챗봇

---

## 1. 실습 개요

이 실습에서는 Amazon Bedrock Agent를 사용해 **정적 문서 검색**과 **실시간 인프라 상태 조회**를 함께 수행하는 지능형 운영 챗봇을 구현한다.

실습 1에서 만든 RAG 기반 지식 서비스는 매뉴얼, 장애 대응 문서, 패치 일정 문서처럼 **이미 저장된 문서**를 기반으로 답변한다.

하지만 운영 환경에서는 문서만으로 답할 수 없는 질문이 많다.

예를 들어 다음 질문을 보자.

```
지금 test-svr 인스턴스 상태는 어때?
```

이 질문은 매뉴얼 문서에 답이 들어 있지 않다.

현재 EC2 상태를 AWS API로 조회해야 답할 수 있다.

반대로 다음 질문은 실시간 API 조회가 아니라 문서 검색이 더 적합하다.

```
정기 패치 일자는 언제야?
```

이 질문은 운영 매뉴얼이나 패치 정책 문서에 들어 있는 정보를 검색해야 한다.

따라서 이번 실습에서는 Bedrock Agent가 질문의 성격을 판단해 다음처럼 동작하게 만든다.

```
사용자 질문
   ↓
Bedrock Agent
   ↓
질문 의도 판단
   ├─ 실시간 EC2 상태 질문 → Lambda 도구 호출
   └─ 매뉴얼/패치/장애 대응 질문 → Knowledge Base 검색
   ↓
최종 답변 생성
```

Amazon Bedrock Agents는 Foundation Model, 데이터 소스, 소프트웨어 애플리케이션, 사용자 대화를 오케스트레이션하고, 필요한 경우 API 호출과 Knowledge Base 조회를 수행할 수 있다.

---

## 2. 실습 목표

이 실습을 완료하면 다음을 수행할 수 있다.

* Bedrock Agent의 역할을 설명할 수 있다.

* Agent가 Lambda 함수를 도구처럼 호출하는 구조를 이해할 수 있다.

* Lambda에서 `boto3`를 사용해 EC2 상태를 조회할 수 있다.

* Agent Action Group을 구성할 수 있다.

* Knowledge Base를 Agent에 연결할 수 있다.

* 하나의 질문 안에서 실시간 운영 상태와 매뉴얼 정보를 함께 답변하게 만들 수 있다.

* Lambda 실행 역할과 Lambda 리소스 기반 정책의 차이를 설명할 수 있다.

---

## 3. 이번 실습에서 만드는 결과물

최종적으로 학생들은 다음과 같은 질문을 Bedrock Agent 테스트 창에 입력한다.

```
지금 test-svr 상태는 어떻고, 정기 패치 일자는 언제야?
```

Agent는 다음 두 가지 작업을 수행한다.

| 질문 부분 | 처리 방식 |
| --- | --- |
| `지금 test-svr 상태는 어때?` | Lambda 함수 호출 후 EC2 현재 상태 조회 |
| `정기 패치 일자는 언제야?` | Knowledge Base에서 운영 매뉴얼 검색 |

예상 답변 형태는 다음과 같다.

```
test-svr 인스턴스의 현재 상태는 running입니다.

정기 패치 일정은 운영 매뉴얼 기준으로 매월 둘째 주 수요일 22:00에 진행됩니다.
따라서 현재 서버는 실행 중이며, 다음 정기 패치 일정에 맞춰 사전 점검을 진행하면 됩니다.
```

실제 패치 일정 문구는 실습 1에서 Knowledge Base에 업로드한 문서 내용에 따라 달라질 수 있다.

---

# 4. 사전 준비

이번 실습은 실습 1에서 만든 Knowledge Base를 활용한다.

따라서 아래 항목이 준비되어 있어야 한다.

| 준비 항목 | 설명 |
| --- | --- |
| AWS CLI | 실습 PC 또는 CloudShell에서 사용 |
| Bedrock 모델 접근 권한 | Nova Lite, Claude 3 Haiku 등 사용 가능 상태 |
| 실습 1 Knowledge Base | 운영 매뉴얼, 패치 일정 문서가 포함된 KB |
| EC2 인스턴스 | `Name=test-svr` 태그가 있는 인스턴스 |
| Lambda 생성 권한 | Lambda 함수 생성 및 IAM 역할 수정 가능 |
| IAM 권한 | EC2 조회, Lambda 권한 추가, Bedrock Agent 구성 가능 |

---

## 4.1 EC2 인스턴스 태그 확인

이번 실습에서는 EC2 인스턴스를 이름으로 조회한다.

EC2 인스턴스에 다음 태그가 있어야 한다.

| Key | Value |
| --- | --- |
| Name | test-svr |

AWS CLI로 확인한다.

```
aws ec2 describe-instances \
--filters"Name=tag:Name,Values=test-svr" \
--query"Reservations[].Instances[].{InstanceId:InstanceId,Name:Tags[?Key=='Name']|[0].Value,State:State.Name}" \
--output table
```

예상 출력은 다음과 같다.

```
------------------------------------------------
|              DescribeInstances               |
+----------------------+-----------+-----------+
|      InstanceId      |   Name    |   State   |
+----------------------+-----------+-----------+
|  i-0123456789abcdef0 | test-svr  | running   |
+----------------------+-----------+-----------+
```

조회 결과가 비어 있으면 EC2 인스턴스 태그를 먼저 확인한다.

---

# 5. 전체 아키텍처

```
사용자
  │
  │ 질문 입력
  ▼
Amazon Bedrock Agent
  │
  ├─ 질문이 "현재 상태", "지금", "서버 상태"와 관련됨
  │      ▼
  │   Action Group
  │      ▼
  │   Lambda 함수 rag-lab
  │      ▼
  │   EC2 DescribeInstances API 호출
  │      ▼
  │   현재 인스턴스 상태 반환
  │
  └─ 질문이 "패치 일정", "장애 대응", "운영 매뉴얼"과 관련됨
         ▼
      Knowledge Base
         ▼
      S3 문서 + 벡터 검색
         ▼
      관련 문서 내용 반환

최종적으로 Agent가 두 결과를 합쳐 자연어 답변 생성
```

---

# 6. 핵심 개념 설명

## 6.1 Bedrock Agent란 무엇인가

Bedrock Agent는 사용자의 질문을 단순히 LLM에 전달하는 역할만 하지 않는다.

Agent는 사용자의 질문을 분석한 뒤, 필요한 작업을 스스로 선택한다.

예를 들어 사용자가 다음처럼 질문했다고 하자.

```
지금 test-svr 상태는 어떻고, 정기 패치 일자는 언제야?
```

이 질문에는 두 가지 의도가 섞여 있다.

첫 번째는 현재 EC2 상태 조회이다.

```
지금 test-svr 상태는 어때?
```

이 질문은 실시간 AWS API 조회가 필요하다.

두 번째는 매뉴얼 검색이다.

```
정기 패치 일자는 언제야?
```

이 질문은 Knowledge Base 검색이 필요하다.

Agent는 이 두 작업을 분리해서 처리하고, 마지막에 하나의 답변으로 합친다.

---

## 6.2 Action Group이란 무엇인가

Action Group은 Agent가 호출할 수 있는 작업 묶음이다.

예를 들어 이번 실습에서는 다음 작업을 Action Group으로 만든다.

```
get_ec2_status
```

이 작업은 EC2 인스턴스 이름을 입력받고, 현재 상태를 반환한다.

Agent는 사용자가 “test-svr 상태 알려줘”라고 질문하면 `get_ec2_status` 기능이 필요하다고 판단하고 Lambda 함수를 호출한다.

Bedrock Agent의 Action Group은 Lambda 함수와 연결될 수 있으며, Agent는 API 스키마 또는 Function details에 정의된 정보를 바탕으로 필요한 입력값을 Lambda로 전달한다.

---

## 6.3 Lambda의 역할

Lambda는 실제 AWS API를 호출하는 실행 코드이다.

이번 실습에서 Lambda는 다음 작업을 수행한다.

```
1. Agent로부터 instance_name 값을 받음
2. EC2 describe_instances API 호출
3. Name 태그가 instance_name과 같은 인스턴스 검색
4. 인스턴스 ID, 이름, 상태, 타입, Private IP 조회
5. 결과를 Agent가 이해할 수 있는 JSON 형식으로 반환
```

---

## 6.4 Knowledge Base의 역할

Knowledge Base는 운영 매뉴얼, 패치 일정, 장애 대응 절차 같은 문서를 검색하는 역할을 한다.

이번 실습에서는 실습 1에서 만든 Knowledge Base를 Agent에 연결한다.

사용자가 다음처럼 질문하면 Knowledge Base가 사용된다.

```
정기 패치 일자는 언제야?
장애 발생 시 1차 조치 절차는 뭐야?
서버 점검 전 확인해야 하는 항목은 뭐야?
```

---

# 7. Step 1. Lambda 함수 생성

## 7.1 Lambda 함수 생성

AWS 콘솔에서 다음 순서로 이동한다.

```
AWS Console
→ Lambda
→ Create function
```

다음과 같이 설정한다.

| 항목 | 값 |
| --- | --- |
| Function name | `rag-lab` |
| Runtime | Python 3.12 또는 Python 3.x |
| Architecture | x86\_64 |
| Execution role | Create a new role with basic Lambda permissions |

함수를 생성한 뒤 Runtime이 Python인지 확인한다.

---

## 7.2 Lambda 코드 작성

Lambda 코드 편집기에 아래 코드를 입력한다.

파일명 개념상 `lambda_function.py`이다.

```
import json
import boto3
from botocore.exceptions import ClientError

ec2 = boto3.client("ec2")

def get_parameter(event, name, default=None):
    """
    Bedrock Agent가 Lambda로 전달한 parameters 목록에서
    특정 이름의 값을 찾아 반환한다.
    """
    parameters = event.get("parameters", [])

    for parameter in parameters:
        if parameter.get("name") == name:
            return parameter.get("value")

    return default

def find_instances_by_name(instance_name):
    """
    EC2 Name 태그를 기준으로 인스턴스를 조회한다.
    terminated 인스턴스는 이미 삭제된 인스턴스이므로 운영 상태 조회 결과에 포함하지 않는다.
    """
    response = ec2.describe_instances(
        Filters=[
            {
                "Name": "tag:Name",
                "Values": [instance_name]
            },
            {
                "Name": "instance-state-name",
                "Values": ["pending", "running", "stopping", "stopped"]
            }
        ]
    )

    instances = []

    for reservation in response.get("Reservations", []):
        for instance in reservation.get("Instances", []):
            name = ""

            for tag in instance.get("Tags", []):
                if tag.get("Key") == "Name":
                    name = tag.get("Value")
                    break

            instances.append({
                "instance_id": instance.get("InstanceId"),
                "name": name,
                "state": instance.get("State", {}).get("Name"),
                "instance_type": instance.get("InstanceType"),
                "private_ip": instance.get("PrivateIpAddress", "N/A"),
                "public_ip": instance.get("PublicIpAddress", "N/A"),
                "availability_zone": instance.get("Placement", {}).get("AvailabilityZone")
            })

    return instances

def build_agent_response(event, response_body):
    """
    Bedrock Agent가 이해할 수 있는 응답 형식으로 결과를 반환한다.
    """
    return {
        "messageVersion": "1.0",
        "response": {
            "actionGroup": event.get("actionGroup"),
            "function": event.get("function"),
            "functionResponse": {
                "responseBody": {
                    "TEXT": {
                        "body": json.dumps(response_body, ensure_ascii=False)
                    }
                }
            }
        },
        "sessionAttributes": event.get("sessionAttributes", {}),
        "promptSessionAttributes": event.get("promptSessionAttributes", {})
    }

def lambda_handler(event, context):
    """
    Lambda 진입점: EC2 인스턴스 조회 후 Agent 응답 형식으로 반환한다.
    """
    print("Received event:")
    print(json.dumps(event, ensure_ascii=False))

    try:
        instance_name = get_parameter(event, "instance_name")

        if not instance_name:
            response_body = {
                "success": False,
                "message": "instance_name 파라미터가 필요함"
            }
            return build_agent_response(event, response_body)

        instances = find_instances_by_name(instance_name)

        if not instances:
            response_body = {
                "success": True,
                "message": f"{instance_name} 이름을 가진 EC2 인스턴스를 찾을 수 없음",
                "instances": []
            }
            return build_agent_response(event, response_body)

        response_body = {
            "success": True,
            "message": f"{instance_name} 인스턴스 상태 조회 완료",
            "instances": instances
        }

        return build_agent_response(event, response_body)

    except ClientError as e:
        response_body = {
            "success": False,
            "message": "AWS API 호출 중 오류 발생",
            "error": str(e)
        }
        return build_agent_response(event, response_body)

    except Exception as e:
        response_body = {
            "success": False,
            "message": "Lambda 처리 중 알 수 없는 오류 발생",
            "error": str(e)
        }
        return build_agent_response(event, response_body)
```

---

## 7.3 Lambda 코드 설명

### `boto3.client("ec2")`

```
ec2=boto3.client("ec2")
```

이 코드는 EC2 API를 호출하기 위한 클라이언트를 생성한다.

Lambda가 실행되는 리전을 기준으로 EC2 API를 호출한다.

예를 들어 Lambda가 `ap-northeast-2` 리전에 있으면 서울 리전의 EC2를 조회한다.

---

### `get_parameter()`

```
defget_parameter(event,name,default=None):
```

Bedrock Agent는 Lambda를 호출할 때 사용자의 질문에서 추출한 값을 `parameters` 배열에 담아 전달한다.

예를 들어 사용자가 다음처럼 질문했다고 하자.

```
test-svr 상태 알려줘
```

Agent는 `test-svr`를 인스턴스 이름으로 판단하고 다음과 비슷한 이벤트를 Lambda에 전달한다.

```
{
  "function":"get_ec2_status",
  "parameters": [
    {
      "name":"instance_name",
      "type":"string",
      "value":"test-svr"
    }
  ]
}
```

`get_parameter(event, "instance_name")`은 이 배열에서 `instance_name` 값을 찾아 반환한다.

---

### `find_instances_by_name()`

```
response=ec2.describe_instances(
Filters=[
        {
"Name":"tag:Name",
"Values": [instance_name]
        }
    ]
)
```

이 코드는 EC2 인스턴스의 태그 중 `Name` 값이 `instance_name`과 같은 인스턴스를 조회한다.

AWS CLI로 보면 다음 명령과 비슷하다.

```
aws ec2 describe-instances \
--filters"Name=tag:Name,Values=test-svr"
```

---

### `build_agent_response()`

```
return {
"messageVersion":"1.0",
"response": {
"actionGroup":event.get("actionGroup"),
"function":event.get("function"),
"functionResponse": {
"responseBody": {
"TEXT": {
"body":json.dumps(response_body,ensure_ascii=False)
                }
            }
        }
    }
}
```

이 부분은 Lambda가 Bedrock Agent에게 결과를 돌려주는 응답 구조이다.

Agent는 이 응답을 읽고 사용자에게 자연어로 다시 정리한다.

AWS 문서에 따르면 Bedrock Agent가 Lambda Action Group을 호출할 때 입력 이벤트와 응답은 지정된 형식을 따라야 하며, Lambda는 입력 이벤트의 값을 사용해 비즈니스 로직을 처리하고 응답을 반환한다.

---

# 8. Step 2. Lambda 실행 역할에 EC2 조회 권한 추가

Lambda 함수는 기본적으로 EC2를 조회할 권한이 없다.

따라서 Lambda 실행 역할에 EC2 읽기 권한을 추가해야 한다.

---

## 8.1 Lambda 실행 역할 이름 확인

Lambda 콘솔에서 확인한다.

```
Lambda
→ rag-lab
→ Configuration
→ Permissions
→ Execution role
```

Role name을 확인한다.

예시는 다음과 같다.

```
rag-lab-role-abc123
```

---

## 8.2 CLI로 권한 추가

아래 명령에서 `[함수_역할_이름]` 부분을 실제 역할 이름으로 바꾼다.

```
aws iam attach-role-policy \
--role-name [함수_역할_이름] \
--policy-arn arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
```

예시는 다음과 같다.

```
aws iam attach-role-policy \
--role-name rag-lab-role-abc123 \
--policy-arn arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
```

---

## 8.3 명령어 설명

```
aws iam attach-role-policy
```

IAM 역할에 관리형 정책을 연결하는 명령이다.

```
--role-name rag-lab-role-abc123
```

정책을 연결할 IAM 역할 이름이다.

여기서는 Lambda 함수가 사용하는 실행 역할을 입력한다.

```
--policy-arn arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
```

AWS 관리형 정책 중 EC2 읽기 전용 권한을 부여한다.

이 권한이 있어야 Lambda 코드에서 다음 API를 호출할 수 있다.

```
ec2.describe_instances()
```

---

# 9. Step 3. Lambda 단독 테스트

Agent에 연결하기 전에 Lambda만 먼저 테스트한다.

이 과정을 생략하면 문제가 발생했을 때 원인을 찾기 어렵다.

---

## 9.1 Lambda 테스트 이벤트 생성

Lambda 콘솔에서 다음을 클릭한다.

```
Test
→ Create new test event
```

Event name은 다음처럼 입력한다.

```
bedrock-agent-test
```

테스트 이벤트는 아래 JSON을 사용한다.

```
{
  "messageVersion":"1.0",
  "actionGroup":"infra-operation-action",
  "function":"get_ec2_status",
  "parameters": [
    {
      "name":"instance_name",
      "type":"string",
      "value":"test-svr"
    }
  ],
  "sessionAttributes": {},
  "promptSessionAttributes": {}
}
```

---

## 9.2 테스트 실행

`Test` 버튼을 클릭한다.

정상 실행되면 다음과 비슷한 결과가 나온다.

```
{
  "messageVersion":"1.0",
  "response": {
    "actionGroup":"infra-operation-action",
    "function":"get_ec2_status",
    "functionResponse": {
      "responseBody": {
        "TEXT": {
          "body":"{\"success\": true, \"message\": \"test-svr 인스턴스 상태 조회 완료\", \"instances\": [{\"instance_id\": \"i-0123456789abcdef0\", \"name\": \"test-svr\", \"state\": \"running\", \"instance_type\": \"t3.micro\", \"private_ip\": \"10.0.1.10\", \"public_ip\": \"N/A\", \"availability_zone\": \"ap-northeast-2a\"}]}"
        }
      }
    }
  },
  "sessionAttributes": {},
  "promptSessionAttributes": {}
}
```

중요한 확인 포인트는 다음이다.

| 확인 항목 | 정상 예시 |
| --- | --- |
| `success` | `true` |
| `name` | `test-svr` |
| `state` | `running` 또는 `stopped` |
| `instance_id` | `i-`로 시작하는 값 |

---

## 9.3 자주 발생하는 오류

### 오류 1. AccessDenied

예시는 다음과 같다.

```
An error occurred (UnauthorizedOperation) when calling the DescribeInstances operation
```

원인은 Lambda 실행 역할에 EC2 조회 권한이 없기 때문이다.

해결 방법은 다음 명령을 다시 확인한다.

```
aws iam attach-role-policy \
--role-name [함수_역할_이름] \
--policy-arn arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
```

---

### 오류 2. 인스턴스를 찾을 수 없음

예시는 다음과 같다.

```
{
  "success":true,
  "message":"test-svr 이름을 가진 EC2 인스턴스를 찾을 수 없음",
  "instances": []
}
```

원인은 보통 다음 중 하나이다.

* EC2 인스턴스의 `Name` 태그가 `test-svr`가 아님

* Lambda와 EC2 인스턴스의 리전이 다름

* 인스턴스가 terminated 상태임

---

# 10. Step 4. Bedrock Agent 생성

## 10.1 Agent 생성

AWS 콘솔에서 다음으로 이동한다.

```
Amazon Bedrock
→ Agents
→ Create Agent
```

Agent 이름은 다음처럼 입력한다.

```
infra-operation-agent
```

설명은 다음처럼 입력한다.

```
EC2 실시간 상태 조회와 운영 매뉴얼 검색을 함께 수행하는 인프라 운영 챗봇
```

AWS 문서 기준으로 Agent를 Prepare하고 테스트하려면 Agent resource role, Foundation Model, Instructions 구성이 필요하고, Action Group 또는 Knowledge Base를 하나 이상 구성하는 것이 일반적이다.

---

## 10.2 모델 선택

모델은 실습 환경에서 사용 가능한 모델을 선택한다.

예시는 다음 중 하나이다.

| 모델 | 특징 |
| --- | --- |
| Amazon Nova Lite | 빠르고 비용 부담이 낮은 실습용 모델 |
| Claude 3 Haiku | 빠른 응답과 안정적인 지시 수행에 적합 |
| Claude 3.5 Haiku | 사용 가능하면 경량 Agent 실습에 적합 |

실습에서는 빠른 응답을 위해 `Nova Lite` 또는 `Claude 3 Haiku` 계열을 권장한다.

---

## 10.3 Agent 지침 입력

Instructions에 아래 내용을 입력한다.

```
너는 클라우드 인프라 운영 전문가이다.

사용자의 질문을 분석해서 실시간 운영 상태 조회가 필요한지, 운영 매뉴얼 검색이 필요한지 판단한다.

다음 규칙을 따른다.

1. EC2 인스턴스의 현재 상태, 실행 여부, 중지 여부, 서버 상태, 지금 상태를 묻는 질문은 get_ec2_status 함수를 사용한다.
2. get_ec2_status 함수를 사용할 때는 사용자가 말한 서버 이름을 instance_name 파라미터로 전달한다.
3. 정기 패치 일정, 장애 대응 절차, 운영 정책, 점검 항목, 매뉴얼 내용은 Knowledge Base를 사용해 검색한다.
4. 하나의 질문에 실시간 상태 조회와 매뉴얼 검색이 함께 포함되어 있으면 두 작업을 모두 수행한 뒤 하나의 답변으로 정리한다.
5. Lambda 함수 결과에 state 값이 있으면 running, stopped, pending, stopping 상태를 사용자가 이해하기 쉽게 설명한다.
6. 모르는 내용은 추측하지 말고 확인 가능한 정보 기준으로 답변한다.
7. 답변은 한국어로 작성한다.
```

# 11. Step 5. Action Group 추가

## 11.1 Action Group 생성

Agent Builder 화면에서 다음을 선택한다.

```
Action groups
→ Add
```

다음과 같이 입력한다.

| 항목 | 값 |
| --- | --- |
| Action group name | `infra-operation-action` |
| Description | `EC2 인스턴스의 현재 실행 상태를 조회하는 운영 도구` |
| Action group type | Define with function details |
| Lambda function | `rag-lab` |

---

## 11.2 Function details 입력

Function 이름은 다음과 같이 입력한다.

```
get_ec2_status
```

Function 설명은 다음처럼 작성한다.

```
EC2 인스턴스 이름을 입력받아 현재 상태, 인스턴스 ID, 인스턴스 타입, Private IP, Availability Zone을 조회한다. 사용자가 서버의 현재 상태, 실행 여부, 중지 여부를 질문할 때 사용한다.
```

---

## 11.3 Parameter 추가

파라미터를 하나 추가한다.

| 항목 | 값 |
| --- | --- |
| Name | `instance_name` |
| Type | `string` |
| Required | `Yes` |
| Description | `조회할 EC2 인스턴스의 Name 태그 값. 예: test-svr` |

Function details는 Agent가 필요한 파라미터 이름, 타입, 필수 여부를 이해하게 하는 설정이다. Agent는 이 정보를 바탕으로 사용자 입력에서 값을 추출하거나 부족한 값을 사용자에게 추가 질문할 수 있다.

---

## 11.4 이 설정이 중요한 이유

사용자가 다음처럼 질문했다고 하자.

```
test-svr 지금 켜져 있어?
```

Agent는 이 질문에서 다음 값을 추출해야 한다.

```
instance_name = test-svr
```

`instance_name` 파라미터 설명이 부정확하면 Agent가 값을 잘못 추출하거나 Lambda를 호출하지 않을 수 있다.

따라서 파라미터 설명에는 반드시 다음 정보를 넣는 것이 좋다.

* 어떤 값을 입력해야 하는지

* 예시 값은 무엇인지

* 언제 이 함수를 사용해야 하는지

---

# 12. Step 6. Knowledge Base 연결

Agent Builder에서 다음 영역으로 이동한다.

```
Knowledge bases
→ Add
```

실습 1에서 만든 Knowledge Base를 선택한다.

예시는 다음과 같다.

```
rag-manual-kb
```

Knowledge Base 설명에는 아래처럼 입력한다.

```
운영 매뉴얼, 정기 패치 일정, 장애 대응 절차, 점검 항목을 검색할 때 사용하는 지식 기반이다.
```

Agent가 Knowledge Base를 사용할 수 있도록 설명을 구체적으로 작성해야 한다.

---

## 12.1 Knowledge Base 설명 예시

아래 설명을 그대로 사용해도 된다.

```
이 Knowledge Base는 인프라 운영 매뉴얼 정보를 포함한다. 정기 패치 일정, 서버 점검 절차, 장애 대응 절차, 운영 정책, S3 업로드 오류 대응, EC2 상태별 조치 방법에 대한 질문이 들어오면 이 지식 기반을 사용한다.
```

---

# 13. Step 7. Agent가 Lambda를 호출할 수 있도록 권한 추가

여기서 중요한 권한이 하나 더 필요하다.

앞에서 추가한 권한은 **Lambda가 EC2를 조회할 수 있는 권한**이었다.

이번에 추가할 권한은 **Bedrock Agent가 Lambda를 실행할 수 있는 권한**이다.

두 권한은 다르다.

| 권한 | 대상 | 목적 |
| --- | --- | --- |
| Lambda 실행 역할 권한 | Lambda → EC2 | Lambda가 EC2 API를 호출 |
| Lambda 리소스 기반 정책 | Bedrock Agent → Lambda | Agent가 Lambda 함수를 실행 |

AWS는 Bedrock Agent가 Lambda를 호출하려면 Lambda 함수에 리소스 기반 정책을 추가해야 한다고 안내한다.

---

## 13.1 Agent ID 확인

Bedrock Agent 상세 화면에서 Agent ID를 확인한다.

예시는 다음과 같다.

```
ABCDEFGHIJ
```

---

## 13.2 AWS 계정 ID 확인

CLI에서 다음 명령을 실행한다.

```
aws sts get-caller-identity \
--query Account \
--output text
```

예상 출력은 다음과 같다.

```
123456789012
```

---

## 13.3 Lambda add-permission 실행

아래 명령에서 값을 수정한다.

```
aws lambda add-permission \
--function-name rag-lab \
--statement-id allow-bedrock-agent-invoke \
--action"lambda:InvokeFunction" \
--principal bedrock.amazonaws.com \
--source-arn"arn:aws:bedrock:ap-northeast-2:[계정ID]:agent/[에이전트ID]"
```

예시는 다음과 같다.

```
aws lambda add-permission \
--function-name rag-lab \
--statement-id allow-bedrock-agent-invoke \
--action"lambda:InvokeFunction" \
--principal bedrock.amazonaws.com \
--source-arn"arn:aws:bedrock:ap-northeast-2:123456789012:agent/ABCDEFGHIJ"
```

---

## 13.4 명령어 설명

```
aws lambda add-permission
```

Lambda 함수에 리소스 기반 권한을 추가한다.

```
--function-name rag-lab
```

권한을 추가할 Lambda 함수 이름이다.

```
--statement-id allow-bedrock-agent-invoke
```

정책 문장 ID이다.

동일한 statement ID를 다시 사용하면 오류가 발생할 수 있다.

이미 추가했다면 다른 ID를 사용하거나 기존 정책을 제거해야 한다.

```
--action"lambda:InvokeFunction"
```

Lambda 함수를 호출할 수 있는 권한이다.

```
--principal bedrock.amazonaws.com
```

Bedrock 서비스가 Lambda를 호출할 수 있도록 허용한다.

```
--source-arn"arn:aws:bedrock:ap-northeast-2:123456789012:agent/ABCDEFGHIJ"
```

특정 Bedrock Agent만 이 Lambda를 호출할 수 있도록 제한한다.

AWS 권장 방식에서는 SourceAccount와 SourceArn 조건을 사용해 신뢰할 수 있는 Bedrock Agent만 Lambda를 호출하게 제한한다. 이는 confused deputy 문제를 줄이는 데 도움이 된다.

---

## 13.5 이미 권한이 있다고 나오는 경우

다음 오류가 발생할 수 있다.

```
ResourceConflictException: The statement id (allow-bedrock-agent-invoke) provided already exists.
```

이 경우 기존 정책을 확인한다.

```
aws lambda get-policy \
--function-name rag-lab
```

기존 statement를 삭제하려면 다음 명령을 사용한다.

```
aws lambda remove-permission \
--function-name rag-lab \
--statement-id allow-bedrock-agent-invoke
```

그 뒤 다시 `add-permission`을 실행한다.

---

# 14. Step 8. Agent Prepare

Agent 설정을 변경한 뒤에는 반드시 Prepare를 실행해야 한다.

Bedrock Agent 콘솔에서 다음을 클릭한다.

```
Prepare
```

Prepare는 현재 Agent 설정, Action Group, Knowledge Base 연결 정보를 테스트 가능한 상태로 반영하는 과정이다.

Prepare를 하지 않으면 방금 추가한 Action Group이나 Knowledge Base가 테스트에 반영되지 않을 수 있다.

---

# 15. Step 9. Agent 테스트

## 15.1 테스트 1. EC2 상태만 질문

테스트 창에 입력한다.

```
test-svr 현재 상태 알려줘
```

예상 답변은 다음과 같다.

```
test-svr 인스턴스의 현재 상태는 running입니다.

인스턴스 ID는 i-0123456789abcdef0이고, 인스턴스 타입은 t3.micro입니다.
Private IP는 10.0.1.10이며, 가용 영역은 ap-northeast-2a입니다.
```

확인할 점은 다음이다.

| 확인 항목 | 정상 동작 |
| --- | --- |
| Lambda 호출 여부 | 호출됨 |
| EC2 상태 포함 여부 | running, stopped 등 표시 |
| 인스턴스 이름 | test-svr |
| 응답 언어 | 한국어 |

---

## 15.2 테스트 2. Knowledge Base 질문

테스트 창에 입력한다.

```
정기 패치 일자는 언제야?
```

예상 답변은 다음과 같다.

```
운영 매뉴얼 기준 정기 패치는 매월 둘째 주 수요일 22:00에 진행됩니다.
패치 전에는 대상 서버 상태 확인, 서비스 영향도 확인, 백업 상태 확인을 수행해야 합니다.
```

이 답변은 실습 1에서 Knowledge Base에 업로드한 문서 내용에 따라 달라진다.

---

## 15.3 테스트 3. 실시간 조회 + 매뉴얼 검색 통합 질문

테스트 창에 입력한다.

```
지금 test-svr 상태는 어떻고, 정기 패치 일자는 언제야?
```

예상 답변은 다음과 같다.

```
test-svr 인스턴스의 현재 상태는 running입니다.

정기 패치 일정은 운영 매뉴얼 기준으로 매월 둘째 주 수요일 22:00입니다.
따라서 현재 서버는 실행 중이며, 패치 일정 전에는 서비스 영향도, 백업 상태, 인스턴스 상태를 사전에 확인하는 것이 좋습니다.
```

이 테스트가 성공하면 이번 실습의 핵심 목표가 완료된 것이다.

---

# 16. 테스트 질문 예시

학생들이 다양한 질문으로 Agent 동작을 확인할 수 있도록 아래 질문을 사용한다.

## 16.1 Lambda 호출 유도 질문

```
test-svr 상태 알려줘
```

```
지금 test-svr 인스턴스 실행 중이야?
```

```
test-svr 서버가 stopped 상태인지 확인해줘
```

```
현재 test-svr의 Private IP도 알려줘
```

---

## 16.2 Knowledge Base 검색 유도 질문

```
정기 패치 일자는 언제야?
```

```
장애 발생 시 1차 조치 절차는 뭐야?
```

```
서버 패치 전에 확인해야 할 항목은 뭐야?
```

```
EC2가 stopped 상태일 때 운영 매뉴얼에서는 어떻게 조치하라고 되어 있어?
```

---

## 16.3 통합 질문

```
지금 test-svr 상태를 확인하고, 패치 전에 확인할 항목도 알려줘
```

```
test-svr 상태는 어떻고, 장애 발생 시 1차 조치 절차는 뭐야?
```

```
현재 test-svr가 실행 중인지 확인하고, 정기 패치 일정도 알려줘
```

---

# 17. 학생 실습 과제

## 과제 1. Lambda 단독 테스트 결과 확인

### 요구사항

Lambda 함수 `rag-lab`을 테스트 이벤트로 실행하고 결과를 확인한다.

### 입력 이벤트

```
{
  "messageVersion":"1.0",
  "actionGroup":"infra-operation-action",
  "function":"get_ec2_status",
  "parameters": [
    {
      "name":"instance_name",
      "type":"string",
      "value":"test-svr"
    }
  ],
  "sessionAttributes": {},
  "promptSessionAttributes": {}
}
```

### 제출 결과

아래 항목을 캡처하거나 기록한다.

```
인스턴스 이름:
인스턴스 ID:
현재 상태:
Private IP:
Availability Zone:
```

---

## 과제 2. Agent에서 EC2 상태 질문하기

### 요구사항

Bedrock Agent 테스트 창에서 다음 질문을 입력한다.

```
test-svr 현재 상태 알려줘
```

### 확인 항목

* Agent가 Lambda를 호출했는가

* 현재 상태가 답변에 포함되었는가

* 인스턴스 ID 또는 Private IP가 포함되었는가

---

## 과제 3. Agent에서 매뉴얼 질문하기

### 요구사항

Bedrock Agent 테스트 창에서 다음 질문을 입력한다.

```
정기 패치 일자는 언제야?
```

### 확인 항목

* Knowledge Base 기반 답변이 나오는가

* 문서에 없는 내용을 추측하지 않는가

* 패치 일정 또는 점검 항목이 포함되는가

---

## 과제 4. 통합 질문 테스트

### 요구사항

Bedrock Agent 테스트 창에서 다음 질문을 입력한다.

```
지금 test-svr 상태는 어떻고, 정기 패치 일자는 언제야?
```

### 확인 항목

| 항목 | 확인 |
| --- | --- |
| EC2 현재 상태 조회 | 성공 / 실패 |
| Knowledge Base 검색 | 성공 / 실패 |
| 두 결과 통합 답변 | 성공 / 실패 |
| 답변이 한국어로 출력 | 성공 / 실패 |

---

# 18. 문제 해결 가이드

## 18.1 Agent가 Lambda를 호출하지 않는 경우

확인할 항목은 다음이다.

| 점검 항목 | 설명 |
| --- | --- |
| Action Group 설명 | EC2 상태 조회용 도구라는 설명이 명확한가 |
| Function 설명 | 현재 상태, 실행 여부, 서버 상태 질문에 사용한다고 되어 있는가 |
| Parameter 설명 | `instance_name`이 EC2 Name 태그 값이라고 설명되어 있는가 |
| Agent Instruction | 실시간 서버 상태는 `get_ec2_status`를 사용하라고 되어 있는가 |
| Prepare 실행 여부 | 설정 변경 후 Prepare를 다시 눌렀는가 |

---

## 18.2 Agent가 Lambda 호출 권한 오류를 내는 경우

가능한 원인은 Lambda 리소스 기반 정책이 없기 때문이다.

다시 실행한다.

```
aws lambda add-permission \
--function-name rag-lab \
--statement-id allow-bedrock-agent-invoke \
--action"lambda:InvokeFunction" \
--principal bedrock.amazonaws.com \
--source-arn"arn:aws:bedrock:ap-northeast-2:[계정ID]:agent/[에이전트ID]"
```

---

## 18.3 Lambda는 실행되지만 EC2 조회가 실패하는 경우

가능한 원인은 Lambda 실행 역할에 EC2 권한이 없기 때문이다.

다시 실행한다.

```
aws iam attach-role-policy \
--role-name [함수_역할_이름] \
--policy-arn arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
```

---

## 18.4 Knowledge Base 답변이 나오지 않는 경우

확인할 항목은 다음이다.

| 점검 항목 | 설명 |
| --- | --- |
| KB 연결 여부 | Agent에 Knowledge Base가 추가되어 있는가 |
| KB Sync 여부 | 문서 업로드 후 동기화했는가 |
| 문서 내용 | 정기 패치 일정 문구가 실제 문서에 있는가 |
| KB 설명 | 패치 일정, 운영 매뉴얼 질문에 사용한다고 작성했는가 |
| Prepare 여부 | KB 연결 후 Prepare를 다시 실행했는가 |

---

# 19. 정리

이번 실습에서는 Bedrock Agent를 사용해 지식 기반 챗봇을 운영 기반 챗봇으로 확장했다.

핵심 흐름은 다음과 같다.

```
정적 문서 질문 → Knowledge Base 검색
실시간 운영 상태 질문 → Lambda 도구 호출
복합 질문 → Agent가 두 작업을 오케스트레이션
```

이번 실습에서 중요한 포인트는 권한 구조이다.

```
Lambda 실행 역할 권한
→ Lambda가 EC2를 조회하기 위한 권한

Lambda 리소스 기반 정책
→ Bedrock Agent가 Lambda를 호출하기 위한 권한
```

이 구조를 이해하면 이후 다음과 같은 운영 자동화 챗봇으로 확장할 수 있다.

* EC2 상태 조회 챗봇

* S3 버킷 정책 점검 챗봇

* CloudWatch 알람 조회 챗봇

* 장애 대응 매뉴얼 기반 챗봇

* 운영 상태 조회 + 문서 검색 통합 챗봇

* FastAPI 백엔드와 연결한 사내 운영 포털형 챗봇